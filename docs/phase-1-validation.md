# Phase 1 Validation

Date: 2026-08-08  
Status: **Complete**

## Completed evidence

### Backend

- Validated with Go 1.26.5, the current patched project baseline.
- `gofmt` reports no unformatted Go files.
- `go vet ./...` passes.
- `go test -race ./...` passes.
- `go build -trimpath ./cmd/api` passes.
- The compiled API starts and returns `200` from `/health/live` and `/api/v1`.
- `govulncheck` v1.6.0 reports no vulnerabilities.

### Frontend

- The committed npm lockfile was generated by `npm install`.
- Prettier check passes.
- ESLint passes with zero warnings.
- TypeScript project type checking passes.
- Two Vitest/Testing Library routing tests pass.
- Vite production build succeeds.
- The production preview serves both `/` and the nested `/parent/children` SPA route.
- `npm audit --audit-level=high` reports zero vulnerabilities.

### Configuration and documentation

- Compose and CI YAML parse successfully.
- Example environment files contain local placeholders only.
- Root commands, first-run instructions, architecture, and contribution guidance are documented.
- `git diff --check` passes.

### Docker and full-stack validation

- Installed Docker Engine 29.7.2, containerd 2.3.3, Buildx 0.36.1, and Compose 5.4.0 from Docker's official Ubuntu 26.04 repository.
- Docker is enabled at boot and the daemon is active using the `overlayfs` storage driver and cgroup v2.
- Compose configuration validation passes.
- API and frontend container images build successfully from their committed Dockerfiles.
- The frontend build runs `npm ci` using the committed lockfile and completes its Vite production build.
- PostgreSQL 16.6 starts healthy and accepts connections to the `family_habit` database.
- API starts healthy; `/health/live`, `/health/ready`, and `/api/v1` succeed through Caddy.
- Caddy serves the React root and the `/parent/children` SPA fallback route.
- PostgreSQL, API, and frontend are private-network services; only Caddy is public, while the development API diagnostic port binds to loopback.
- PostgreSQL, API, frontend, and Caddy use `unless-stopped` restart policies; PostgreSQL, API, and frontend report healthy after recreation.

The first image build exposed an npm 9/npm 10 lockfile inconsistency. The lockfile was regenerated with the exact pinned Node 22.14.0/npm 10.9.2 build image, after which the clean container build passed.

## Deferred evidence

CI is intentionally not part of the current Phase 1 gate. The workflow file remains prepared but unvalidated until a remote repository and CI policy are ready. Migration execution will be added after Phase 2 creates the migration harness.

## Host maintenance note

Ubuntu reported that kernel `7.0.0-29-generic` is installed while the VPS is still running `7.0.0-27-generic`. No reboot was performed because rebooting requires separate product-owner approval.
