# Local development defaults. Copy to .env and keep real secrets uncommitted.
APP_ENV=development
HTTP_ADDR=:8080
DATABASE_URL=postgres://family_habit:family_habit_dev@postgres:5432/family_habit?sslmode=disable
READINESS_TIMEOUT=2s
SHUTDOWN_TIMEOUT=10s
COOKIE_SECURE=false
